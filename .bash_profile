# export REACT_APP_API_BASE=http://localhost:4000/api
export REACT_APP_API_BASE=https://tuiter-node-server-app-pme8.onrender.com/api



# unzip the server to a local file system and add the right commands to your operating PATH. On macOS, unzip the file into /usr/local which should create a directory such as /usr/local/mongodb-macos-x86_64-5.0.3 (your version might differ). To be able to execute the database related commands, add the path to the .bash_profile located in your home directory.
export PATH="$PATH:/usr/local/mongodb-macos-aarch64-6.0.5/bin"